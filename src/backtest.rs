// pub fn backtest<'a>(
//     symbol: String,
//     s: &'a SETTINGS_TRADE,
//     src: &[Vec<f64>],
//     w_max: usize,
//     fa_ind: &FA<SETTINGS_INDS, Box<dyn Indicator>>,
//     fa_signals_ready: &FA<SETTINGS_INDS, Box<dyn SignalReady>>,
//     fa_signals_train: &FA<SETTINGS_INDS, Box<dyn SignalTrain>>,
// ) -> StatCollector<'a> {
//     let mut stat_collector = StatCollector::new(symbol, s);
//     let mut trade_cell = TradeCell::new(s.capital, src[src.len() - 1], src_l)

//     for series in src {

//     }
// }
